/*package com.example.demo.Models;

import com.example.demo.Model.Contact;
import com.example.demo.Model.User;

import org.junit.Assert;
import org.junit.jupiter.api.Test;

public class ContactModelTest {

    @Test
    public void checkContact(){
        Contact contact = new Contact();
        contact.setId(6L);
        Assert.assertEquals(contact.getId()==6L, true);
        contact.setFecha("2022-02-22");
        Assert.assertEquals(contact.getFecha().equals("20/02/2022"), true);
        contact.setBody("coucou");
        Assert.assertEquals(contact.getBody().equals("coucou"), true);
        contact.setIdUser(6L);
        Assert.assertEquals(contact.getIdUser() == 6L, true);
    }

}*/
