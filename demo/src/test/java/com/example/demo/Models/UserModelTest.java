/*package com.example.demo.Models;

import com.example.demo.Model.User;

import org.junit.Assert;
import org.junit.jupiter.api.Test;

public class UserModelTest {

    @Test
    public void checkUser(){
        User user = new User();
        user.setId(6L);
        Assert.assertEquals(user.getId()==6L, true);
        user.setEmail("p.pat@hotmai.com");
        Assert.assertEquals(user.getEmail().equals("p.pat@hotmai.com"), true);
        user.setPassword("qwerty1234");
        Assert.assertEquals(user.getPassword().equals("qwerty1234"), true);
    }
    
}*/
